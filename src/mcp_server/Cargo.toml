[package]
name = "hakana-mcp-server"
version = "0.1.0"
edition = "2024"

[lib]
path = "lib.rs"

[dependencies]
hakana-analyzer = { path = "../analyzer" }
hakana-protocol = { path = "../protocol" }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
