[package]
name = "hakana-logger"
version = "0.1.0"
edition = "2024"

[lib]
path = "lib.rs"

[dependencies]
anyhow = "1"
log = "0.4"
log4rs = { version = "1", features = ["gzip"] }
tokio = { version = "1.26.0", features = ["sync"] }
