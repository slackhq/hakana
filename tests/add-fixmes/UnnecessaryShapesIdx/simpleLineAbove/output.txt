type shape_t = shape(
	'a' => ?string,
	'always' => string,
	?'nullable' => string
);
function foo(shape_t $s) {
	/* HAKANA_FIXME[UnnecessaryShapesIdx] Shapes::idx($s, 'always') indexes a nonnull shape field, this can be done by indexing the shape directly with $s['always'] */
	$type = Shapes::idx($s, 'always');
	/* HAKANA_FIXME[UnnecessaryShapesIdx] Shapes::idx($s, 'a') indexes a nonnull shape field, this can be done by indexing the shape directly with $s['a'] */
	$type = Shapes::idx($s, 'a');
	$type = Shapes::idx($s, 'b');
	$type = Shapes::idx($s, 'nullable');

	/* HAKANA_FIXME[UnnecessaryShapesIdx] Shapes::idx($s, 'always') indexes a nonnull shape field, this can be done by indexing the shape directly with $s['always'] */
	$type = Shapes::idx($s, 'always', "useless default");
	$type = Shapes::idx($s, 'nullable', "default");
}

function bar(?shape_t $s) {
	$type = Shapes::idx($s, 'always');
}

function baz(shape(?'maybe_defined' => shape('conditionally_defined' => string)) $s) {
	$a = Shapes::idx($s, 'maybe_defined', shape());
	$b = Shapes::idx($a, 'conditionally_defined');
}