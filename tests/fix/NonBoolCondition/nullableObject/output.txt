function some_cond(int $i): bool {
    return $i++ % 2 === 1;
}

final class C {}

function maybe_object(bool $test): ?C {
    return $test ? new C() : null;
}

function main(bool $input, int $x, string $foo): int {
    if ($x && $input && maybe_object($input) is nonnull && some_cond($x)) {
        echo "test";
    }

    if ($input || maybe_object($input) is null || !some_cond($x)) {
        echo "test";
    }

    if (maybe_object($input) is nonnull) {
        echo "test";
    }

    if (maybe_object($input) is null) {
        echo "test";
    }

    if ($input && !(maybe_object($input) is nonnull || some_cond($x))) {
        echo "test";
    }

    if (!$x) {
        echo "test";
    }

    $bar = 4;
    if (rand(0,1) > 0) {
        $bar = 'baz';
    }

    if ($bar) {
        echo "test";
    }

    return maybe_object($input) is nonnull ? 5 : 4;
}

function newly_supported_types(
    ?(function(): void) $closure,
    ?classname<C> $classname,
    ?class<C> $class_ptr,
    ?typename<C> $typename,
    ?Awaitable<int> $awaitable,
    ?shape('value' => int) $shape,
    ?(int, string) $tuple,
): void {
    if ($closure is null) {
        echo "test";
    }

    if ($classname is nonnull) {
        echo "test";
    }

    if ($class_ptr is null) {
        echo "test";
    }

    if ($typename is nonnull) {
        echo "test";
    }

    if ($awaitable is null) {
        echo "test";
    }

    if ($shape is nonnull) {
        echo "test";
    }

    if ($tuple is null) {
        echo "test";
    }
}
