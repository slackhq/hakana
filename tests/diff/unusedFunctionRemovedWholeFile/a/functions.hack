namespace Bar;

function foo(): void {
    echo "a";
}

final class A extends B {
    const string PRETTY_NAME = "Event Triggered by Campaign redemption during purchase.";
    const bool ONLY_SEND_ONE_NOTIFICATION = true;
    const vec<string> SubscribedValidatedExecutableNotifications = vec[];
    const vec<string> SubscribedExecutableNotifications = vec[];
}